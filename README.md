# Sound Pixels

A microphone-reactive pixel visualizer built with TypeScript, Canvas, and the Web Audio API.

## Build

```sh
npm install
npm run build
```

Then serve the repository with a local HTTP server. Microphone access requires HTTPS in production; GitHub Pages provides it automatically.
